{
  "type": "object",
  "title": "Authorization configuration",
  "required": [
    "#api_token"
  ],
  "properties": {
    "#api_token": {
      "type": "string",
      "title": "API Token",
      "propertyOrder": 1,
      "description" : "Private API Key with Read-Only access to all Klaviyo Objects",
      "format": "password"
    }
  }
}
